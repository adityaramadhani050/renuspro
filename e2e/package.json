{
  "name": "renuspro-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Suite E2E Playwright untuk RenusPro (dijalankan terhadap deploy live).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "smoke": "playwright test tests/01-login.spec.js tests/02-nav-smoke.spec.js",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "dotenv": "^16.4.5"
  }
}
